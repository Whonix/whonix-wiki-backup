Since OpenVPN will be run under user <code>tunnel</code>, that user requires read access to the folder <code>/etc/openvpn</code>.

{{CodeSelect|code=
sudo chown -R tunnel:tunnel /etc/openvpn
}}

{{CodeSelect|code=
sudo chown -R tunnel:tunnel /run/openvpn
}}