Check if you really got the version you want.

{{CodeSelect|code=
git describe
}}

The output should show.

<div class="pre">{{{version}}}</div>

Check if the source folder is pristine.

{{CodeSelect|code=
git status
}}

The output should show nothing.

<div class="pre">HEAD detached at {{{version}}}<br />
nothing to commit, working tree clean</div>

If it shows something else, do not continue.