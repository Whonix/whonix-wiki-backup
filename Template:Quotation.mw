<blockquote class="quotation">{{{quote}}}{{#if: {{{image|}}}{{{context|}}} | <span class="blockquote-context">{{#if: {{{image|}}} | [[File:{{{image}}}|link=|class=author-image]]}}{{{context}}}</span>}}</blockquote><noinclude>
[[Category:MultiWiki]]

{{mbox
| icon = fa-solid fa-info cs-red-light
| text = '''NOTE''' : This template '''may not be used in lists (numbered / bullet points) at the moment'''. It does not work there as expected. This is caused by a bug in the MediaWiki parser that has been reported.
}}
</noinclude>