Restart <code>resolvconf</code>. <ref>
This ensures changes in <code>/etc/resolvconf/run/interface/original.resolvconf</code> from the [[#DNS Configuration|DNS Configuration]] section take effect.
</ref>

{{CodeSelect|code=
sudo service resolvconf restart
}}