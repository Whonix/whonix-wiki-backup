In order to verify the {{project_name_long}} image, GnuPG must be installed. GnuPG is the common OpenPGP implementation for Linux: it is installed by default in Debian, Ubuntu, {{project name long}} and many other distributions.<noinclude>[[Category:MultiWiki]]</noinclude>