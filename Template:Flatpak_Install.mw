Install <code>{{{package}}}</code> via [[Install_Software#Flatpak|flatpak]].

{{Box|text=
'''1.''' Select your platform.
{{Tab
|type=controller
|content=
{{Tab
|active=true
|title= === A : {{non_q_project_name}} ===
|content=
<div>{{non_q_project_name}}</div>
'''2.''' Add <code>flathub</code> repository. <ref>
https://flatpak.org/setup/Debian/
</ref>
{{CodeSelect|code=
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
}}

'''3.''' Install the flatpak <code>{{{package}}}</code> package.
{{CodeSelect|code=
flatpak install flathub {{{package}}}
}}

'''4.''' Done.

The procedure of installing <code>{{{package}}}</code> is complete.

'''5.''' Upgrades notice.

<u>Note</u>: this procedure will not keep the software up-to-date. How to update installation installed by flatpak is also documented on the [[Operating System Software and Updates]] wiki page.
}}
{{Tab
|title= === B : {{q_project_name}} Template ===
|content=
<div>{{q_project_name}} Template (<code>{{project_name_workstation_template}}</code>)</div>
'''2.''' Add <code>flathub</code> repository. <ref>
https://flatpak.org/setup/Debian/
</ref>
{{CodeSelect|code=
http_proxy=http://127.0.0.1:8082 flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
}}

'''3.''' Install the flatpak <code>{{{package}}}</code> package.
{{CodeSelect|code=
http_proxy=http://127.0.0.1:8082 flatpak install flathub {{{package}}}
}}

'''4.''' Done.

The procedure of installing <code>{{{package}}}</code> is complete.

'''5.''' Upgrades notice.

<u>Note</u>: this procedure will not keep the software up-to-date. How to update installation installed by flatpak is also documented on the [[Operating System Software and Updates]] wiki page.
}}
{{Tab
|title= === C : {{q_project_name}} App Qube ===
|content=
<div>{{q_project_name}} App Qube (<code>{{project_name_workstation_vm}}</code>)</div>
'''2.''' Add <code>flathub</code> repository. <ref>
https://flatpak.org/setup/Debian/
</ref>
{{CodeSelect|code=
flatpak --user remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
}}

'''3.''' Install the flatpak <code>{{{package}}}</code> package.
{{CodeSelect|code=
flatpak --user install flathub {{{package}}}
}}

'''4.''' Done.

The procedure of installing <code>{{{package}}}</code> is complete.

'''5.''' Upgrades notice.

<u>Note</u>: this procedure will not keep the software up-to-date. How to update installation installed by flatpak is also documented on the [[Operating System Software and Updates]] wiki page.
}}
}} <!-- End Tab Controller -->
}}<noinclude>
{{Header}}
{{Footer}}
[[Category:MultiWiki]]
</noinclude>