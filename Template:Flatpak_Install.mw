Install <code>{{{package}}}</code> via [[Install_Software#flatpak|flatpak]].

{{Box|text=
'''1.''' Add <code>flathub</code> repository. <ref>
https://flatpak.org/setup/Debian/
</ref>

{{non_q_project_name}}:

{{CodeSelect|code=
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
}}

{{q_project_name}} (in the <code>{{project_name_workstation_vm}}</code> App Qube):

{{CodeSelect|code=
flatpak --user remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
}}

'''2.''' Install the flatpak <code>{{{package}}}</code> package.

{{non_q_project_name}}:

{{CodeSelect|code=
flatpak install flathub org.{{{package}}}.{{{package}}}
}}

{{q_project_name}} (in the <code>{{project_name_workstation_vm}}</code> App Qube):

{{CodeSelect|code=
flatpak --user install flathub org.{{{package}}}.{{{package}}}
}}

Done. The procedure of installing <code>{{{package}}}</code> is complete.

'''3.''' Upgrades notice.

<u>Note</u>: this procedure will not keep the software up-to-date. How to update installation installed by flatpak is also documented on the [[Operating System Software and Updates]] wiki page.
}}