Note that UDP-style VPN connections are incompatible with Tor; the VPN must be configured to use TCP. <ref name=tor_no_udp>
See [[Tor#UDP|UDP]].
</ref> This requires adding <code>proto tcp</code> to the VPN configuration file <code>{{{1}}}</code>. Nearly all VPN providers support this configuration.